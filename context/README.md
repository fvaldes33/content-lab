# Context

Run the `onboard` skill to create `profile.md` here. Nothing else in this system works well until it exists.

The brand pack (for a product or company) lives in `brand/` at the repo root; the same `onboard` skill writes it.
